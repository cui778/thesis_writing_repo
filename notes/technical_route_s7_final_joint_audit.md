# 技术路线图 S7-FINAL-JOINT-AUDIT：最终联合审核

## 1. 审核对象

最终图片：

`E:\11.16\thesis_writing_repo\figures\technical_route\s5_formal_candidates\CANDIDATE-C_concise-thesis-page.png`

配套材料：

`E:\11.16\thesis_writing_repo\notes\technical_route_s6_final_select.md`

## 2. 审核结论

```text
IMAGE-REPAIR
```

总体版式、章节递进和核心技术事实成立，不需要重新选择方向。

但图片中的蓝灰色 `residual` 交付箭头未满足正式候选合同的准确落点要求。完成最小图像修复后，应再次执行联合审核。

## 3. 分项审核

| 审核项 | 结论 | 说明 |
|---|---|---|
| 技术事实 | `PASS` | 数据生成、缺陷诊断与布局优化三部分关系成立 |
| 术语一致性 | `TEXT-REPAIR` | 图内若干短标签可进一步与正文统一 |
| 箭头方向 | `PASS` | residual 箭头向下并向左，橙色桥梁从左向右并向上 |
| 箭头落点 | `IMAGE-REPAIR` | 蓝灰色 residual 箭头停在第 4 章区域上缘，没有准确进入输入卡片 |
| 章节递进 | `PASS` | 第 3 章数据底座、第 4 章诊断、第 5 章优化关系清楚 |
| 核心创新可见性 | `PASS` | `水力传播关联建模` 使用深绿色强调 |
| 图注分工 | `PASS` | 图注承担 I/E、residual、稀疏观测和固定协议说明 |
| 页面可读性 | `PASS` | 横向整页结构清楚，留白充足，标签可读 |

## 4. 必须修复的图像问题

### 4.1 residual 箭头落点

当前表现：

```text
原始特征 + residual 派生特征
-> 蓝灰色折线向下并向左
-> 箭头尖端停在第 4 章虚线区域上缘
```

应修复为：

```text
原始特征 + residual 派生特征
-> residual 特征交付
-> 箭头尖端准确接触“稀疏观测时空图输入”卡片上边缘
```

要求：

- 保留蓝灰色；
- 保留向下并向左的折线路径；
- 将箭头继续向下延伸至输入卡片；
- 不进入第 5 章；
- 不直接进入诊断模型内部。

### 4.2 橙色桥梁

当前橙色桥梁已经满足要求：

```text
固定布局诊断验证
-> 诊断评价驱动布局优化
-> 监测节点布局优化
```

修复 residual 箭头时不得改变该桥梁。

## 5. 建议同步统一的图内短标签

以下属于文字微调，不改变结构：

| 当前图内标签 | 建议标签 |
|---|---|
| 图结构 | 管网图结构 |
| 滑动窗口 | 时序滑动窗口 |
| 固定协议 | 固定诊断协议 |
| 响应信息 | 管网响应信息 |
| 有限资源 | 有限监测资源 |
| 拓扑布局 | 拓扑结构布局 |
| 性能评价与机制分析 | 布局性能评价与机制分析 |
| 正常工况 | 无缺陷正常工况 |

若页面空间不足，允许保留部分短标签，但应优先统一 `管网图结构`、`固定诊断协议` 和 `拓扑结构布局`。

## 6. 标题审核

论文图名：

```text
图 X-X 面向城市排水管网渗入与渗漏缺陷的时空诊断及监测布局优化技术路线
```

结论：

```text
PASS
```

图内标题：

```text
面向城市排水管网渗入与渗漏缺陷的时空诊断及监测布局优化研究
```

结论：

```text
PASS
```

## 7. 图注审核

S6 图注能够正确说明：

- 工程资料与现场监测如何进入 SWMM/PySWMM 仿真；
- 无缺陷正常工况与 I/E 缺陷场景如何形成全网节点级时序；
- residual 派生特征如何进入稀疏观测诊断；
- `p_active(t)` 与 `node_scores(t)` 如何经场景级聚合形成工程输出；
- 固定布局诊断验证如何支撑布局优化；
- 第 5 章固定诊断协议，仅改变监测节点集合及 observed mask。

结论：

```text
PASS
```

## 8. 图例审核

S6 图例与图片整体一致。

需要在图像修复后确认：

```text
蓝灰色粗折线箭头
确实落入第 4 章“稀疏观测时空图输入”
```

结论：

```text
IMAGE-REPAIR 后复核
```

## 9. 正文引用句审核

正文首次引用句：

```text
本文围绕多场景数据生成、水力约束时空图缺陷诊断和诊断导向监测布局优化三个环节展开研究，
总体技术路线如图 X-X 所示。
```

章节衔接句：

```text
如图 X-X 所示，第 3 章构建的原始特征与 residual 派生特征为第 4 章稀疏观测条件下的
时空图缺陷诊断提供输入；第 4 章固定布局诊断验证结果进一步作为第 5 章监测节点布局优化的评价依据。
```

结论：

```text
PASS
```

## 10. 最小修复路线

无需返回 S1、S2、S3 或 S4。

建议：

```text
基于 CANDIDATE-C 仅修复 residual 箭头落点
+ 可选同步统一图内短标签
-> 再次执行 S7-FINAL-JOINT-AUDIT
```

## 11. 工作流状态

```text
当前状态：
IMAGE-REPAIR

工作流是否完成：
否

阻塞项：
蓝灰色 residual 箭头落点需下移至第 4 章输入卡片
```
